#include <stdio.h>
/**
 * main - entry point into the program
 *
 * Return: prints all single digit base 10 umbers from 0
 */
int main(void)
{
	int x = 0;

	while (x < 10)
	{
		printf("%d", x);
		x++;
	}
	return (0);
}
