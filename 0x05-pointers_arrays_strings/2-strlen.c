#include "main.h"

/**
 * _strlen - prints the length of a string
 * @s - the string whose length will be printed
 * Return: 1
 */

int _strlen(char *s)
{
	int len;

	len = sizeof(s);

	return (len);
}
